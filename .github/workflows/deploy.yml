name: Deploy Portfolio

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2  # Esta línea clona el repositorio
      with:
        fetch-depth: 0
        
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: latest
        
    - name: List directory contents
      run: ls -al
        
    - name: Install dependencies
      run: npm install
      
    - name: Build Vue app
      run: npm run build
      
    - name: Upload ftp
      uses: sebastianpopp/ftp-action@releases/v2
      
      with:
        host: 178.33.156.24
        user: nacho@perroagua.com
        password: KVOTHe1973
        localDir: "dist"
